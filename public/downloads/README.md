# Mobile APK downloads

Drop the EAS-built Android APK here as `achtmart.apk`.
The admin `/admin/download` page links to `/downloads/achtmart.apk`.
While in testing, this page is admin-only (not linked from the public footer).

To build: see `../../../achtmart-mobile/README.md`.
