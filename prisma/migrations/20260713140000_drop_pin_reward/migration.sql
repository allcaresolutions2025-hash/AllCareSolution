-- The purchaser-claimed 2000-pt pin reward was replaced by an automatic credit
-- to the NEW member's payout wallet at enrollment time (see the members route).
-- The PinReward table is no longer used.
DROP TABLE IF EXISTS "PinReward";
