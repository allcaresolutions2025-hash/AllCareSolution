// The 2000-pt pin reward. A member who obtains a 2000-pt pin can claim, once per
// such pin, PIN_REWARD_POINTS points into their payout wallet (balanceAvailable)
// after admin approval. The pin denomination that unlocks it is
// PIN_REWARD_PIN_VALUE.
export const PIN_REWARD_PIN_VALUE = 2000;
export const PIN_REWARD_POINTS = 2000;
