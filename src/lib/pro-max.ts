// Master switch for the Pin Pro Max program. Set to `false` to hide/disable the
// entire Pro Max feature (nav links, dashboard widgets, admin sections, the
// nightly Pro Max payout, rewards combo, etc.) without deleting any code — flip
// it back to `true` to fully restore the feature.
export const PRO_MAX_ENABLED = false;
